# sebastianwebsite

Ricky Sebastian | R Events — Master of Ceremonies & event management portfolio.

## Run locally

1. Install dependencies: `npm install`
2. Run: `npm run dev`
3. Build: `npm run build`
